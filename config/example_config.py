"""
Config file used for both build.py and install.py
"""

CONFIG = {
    "sass-path": "sass",
    "jp-mining-note": {
        # rooted under (root)/config
        "opts-path": "jpmn_opts.jsonc",
    },
}
